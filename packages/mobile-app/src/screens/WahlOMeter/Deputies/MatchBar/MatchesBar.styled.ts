import { ChartLegend as ChartLegendCmp } from '@democracy-deutschland/ui';
import Svg from 'react-native-svg';
import styled from 'styled-components/native';

export const Wrapper = styled.View`
  align-items: center;
`;

export const BarWrapper = styled(Svg)`
  margin-top: 5px;
  align-items: center;
`;

export const ChartLegendWrapper = styled.View``;

export const ChartLegend = styled(ChartLegendCmp)``;
