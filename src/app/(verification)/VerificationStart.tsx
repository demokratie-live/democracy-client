import { FC } from "react";
import { VerificationScreen } from "../../screens/Verification";

const VerificationStart: FC = (props) => {
  return <VerificationScreen />;
};

export default VerificationStart;
