git_url("https://github.com/ManAnRuck/DEMOCRACY-match.git")

storage_mode("git")

type(ENV["MATCH_TYPE"] || "development")

app_identifier(["de.democracy-deutschland.clientapp", "de.democracy-deutschland.clientapp.internal"])

force_for_new_devices(true)